```javascript
  npm install && gulp
```
Then add a file in scripts named secret.js & add your api token & project id in variables like so:


```javascript
  module.exports = {
    apiToken : "your token",
    projectId : "your id"
  }
```
